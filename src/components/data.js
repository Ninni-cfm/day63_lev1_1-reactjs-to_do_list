// use import data from './components/data';

let i = 0;
const data = [
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
    { id: i, completed: i % 2 === 0, task: `Task ${++i}` },
];

export default data;